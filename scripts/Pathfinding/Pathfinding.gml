/// @desc
/// @param target: obj or ID to travel to
/// @param except: array of attributes to avoid i.e. "wall"
function Path_To_Closest(target, except){
	var _path = ds_list_create();
	ds_list_destroy(_path);
	return _path;
	
	show_debug_message("this happens");
}

function Path_To_Coord(_x, _y, except){
}